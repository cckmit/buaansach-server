logging:
  level:
    vn.com.bas: DEBUG
    org:
      hibernate:
        SQL: DEBUG
server:
  port: 8888
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bas_prod?useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true&serverTimezone=UTC&useLegacyDatetimeCode=false
    username: root
    password: admin
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false
    time-zone: UTC
  jpa:
    show-sql: true
  liquibase:
    change-log: classpath:config/liquibase/master.xml
    drop-first: false
    enabled: true
